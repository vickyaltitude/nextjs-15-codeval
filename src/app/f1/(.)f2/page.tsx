export default function F2() {
  return <h3>This is intercepted F2</h3>;
}

//so (.)is the file name prefix to use intercepted route.. it should be declared in the source folder and use (.)(..)(...) based on where the target file is
